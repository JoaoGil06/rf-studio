# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
CLAUDE.md
.claude/
.impeccable/
docs/

# `src/graphql/generated/` é versionado de propósito: o build da imagem de
# produção tem contexto `./frontend` e não alcança `../backend`, logo não pode
# correr `pnpm codegen`. O CI corre o codegen e falha se houver diferenças —
# o schema do backend continua a ser a fonte de verdade.